# vim: ft=sshconfig
#
# SSH agent-related config.
#
# Actual ssh-agent execution and such is handled elsewhere (try dotfiles/shell,
# or perhaps my Arch sparker).

# always add keys to agent without confirmation
# (remember to configure the default lifetime you want)
AddKeysToAgent yes
