#!/bin/bash

window_name="Terminator Fullscreen"
wmctrl -r "${window_name}" -t 1
wmctrl -a "${window_name}"
if [ $? -ne 0 ]; then
	(terminator -T "${window_name}" -e "wmctrl -r ${window_name} -t 1; wmctrl -r ${window_name} -b add,fullscreen; exec bash" >& /dev/null) &
fi
exit
