# for xdg-desktop-portal-wlr (now just works & uses slurp to select screen!)
exec dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP=sway

exec $terminal -e "tmuxinator start desktop-autostart"
